# 🎂 Birthday Reminder Bot

Телеграм-бот для напоминания о днях рождения друзей. Учебный проект на Python с использованием pyTelegramBotAPI и MySQL.

## ✨ Возможности

- ➕ Добавление дней рождения с указанием имени и даты
- 📋 Просмотр полного списка сохраненных дней рождения
- 🔔 Показ ближайших дней рождения (на 30 дней вперед)
- 🗑️ Удаление записей
- ⏰ Автоматические уведомления в день рождения
- 📅 Напоминания за N дней до дня рождения
- 🎂 Автоматический расчет возраста (при указании года рождения)
- 🛡️ Защита от спама (rate limiting)
- 📝 Подробное логирование всех действий

## 🛠 Технологии

- **Python 3.8+**
- **pyTelegramBotAPI 4.29.1** - библиотека для работы с Telegram Bot API
- **MySQL 8.0+** - база данных для хранения информации
- **mysql-connector-python** - драйвер для работы с MySQL
- **APScheduler** - планировщик для автоматических уведомлений
- **python-dotenv** - управление переменными окружения

## 📁 Структура проекта

```
tgbot/
├── main.py                      # Точка входа приложения
├── bot.py                       # Инициализация бота
├── config.py                    # Конфигурация и настройки
├── requirements.txt             # Зависимости проекта
├── .env.example                 # Пример файла с переменными окружения
├── .gitignore                   # Игнорируемые файлы
├── database/
│   ├── __init__.py
│   ├── db.py                   # Подключение к БД и connection pooling
│   └── models.py               # SQL запросы и модели данных
├── handlers/
│   ├── __init__.py
│   ├── commands.py             # Обработчики базовых команд (/start, /help)
│   └── birthdays.py            # Обработчики команд работы с ДР
├── keyboards/
│   ├── __init__.py
│   ├── reply_keyboards.py      # Reply-клавиатуры
│   └── inline_keyboards.py     # Inline-клавиатуры
└── utils/
    ├── __init__.py
    ├── scheduler.py             # Планировщик уведомлений
    └── rate_limiter.py          # Защита от спама
```

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/shxpe0x/tgbot.git
cd tgbot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/MacOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных MySQL

```sql
CREATE DATABASE birthdays_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'bot_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON birthdays_db.* TO 'bot_user'@'localhost';
FLUSH PRIVILEGES;
```

### 5. Настройка переменных окружения

Создай файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполни файл `.env` своими данными:

```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_bot_token_here

# Database Configuration
DB_HOST=localhost
DB_USER=bot_user
DB_PASSWORD=your_password
DB_NAME=birthdays_db
DB_POOL_SIZE=5

# Notification Settings
NOTIFICATION_HOUR=9
NOTIFICATION_MINUTE=0
```

### 6. Запуск бота

```bash
python main.py
```

## 📖 Использование

### Команды бота

- `/start` - Запуск бота и отображение главного меню
- `/help` - Справка по доступным командам
- `/add` - Добавить новый день рождения
- `/list` - Показать все сохраненные дни рождения
- `/upcoming` - Показать ближайшие дни рождения (30 дней)
- `/delete` - Удалить день рождения из списка

### Примеры использования

#### Добавление дня рождения

1. Отправь команду `/add`
2. Введи имя друга: `Иван`
3. Введи дату рождения: `15.03.2000` или `15.03` (без года)
4. Подтверди данные

#### Просмотр списка

Отправь `/list` для просмотра всех сохраненных дней рождения с возрастом (если указан год).

#### Ближайшие дни рождения

Отправь `/upcoming` чтобы увидеть дни рождения на ближайшие 30 дней.

## 🗄️ Структура базы данных

### Таблица `users`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| telegram_id | BIGINT | ID пользователя в Telegram |
| username | VARCHAR(255) | Username пользователя |
| created_at | TIMESTAMP | Дата регистрации |

### Таблица `birthdays`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ |
| user_id | INT | ID пользователя (FK) |
| friend_name | VARCHAR(100) | Имя друга |
| birth_date | DATE | Дата рождения |
| birth_year | INT | Год рождения (опционально) |
| remind_days_before | INT | За сколько дней напоминать |
| created_at | TIMESTAMP | Дата создания записи |

## 🎯 Архитектурные особенности

### Connection Pooling

Используется пул соединений MySQL для оптимизации работы с БД:

```python
connection_pool = pooling.MySQLConnectionPool(
    pool_name="bot_pool",
    pool_size=5,
    host=DB_CONFIG['host'],
    user=DB_CONFIG['user'],
    password=DB_CONFIG['password'],
    database=DB_CONFIG['database']
)
```

### Rate Limiting

Защита от спама реализована через декоратор:

```python
@rate_limit(seconds=2)
def cmd_add(message):
    # обработка команды
```

### FSM (Finite State Machine)

Многошаговые диалоги реализованы через систему состояний:

```python
user_states[chat_id] = 'waiting_name'
user_data[chat_id] = {'name': 'Иван'}
```

### Планировщик уведомлений

APScheduler проверяет дни рождения ежедневно в заданное время:

```python
scheduler.add_job(
    check_birthdays,
    'cron',
    hour=9,
    minute=0
)
```

## 📝 Логирование

Все действия бота логируются в файл `bot.log` и в консоль:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO,
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler(sys.stdout)
    ]
)
```

## 🔒 Безопасность

- ✅ Prepared statements для защиты от SQL-инъекций
- ✅ Переменные окружения для хранения секретов
- ✅ Rate limiting для защиты от спама
- ✅ Foreign keys и каскадное удаление
- ✅ Обработка всех исключений

## 🐛 Troubleshooting

### Ошибка подключения к БД

```
mysql.connector.errors.DatabaseError: Can't connect to MySQL server
```

**Решение:** Проверь что MySQL запущен и данные в `.env` корректны.

### Бот не отвечает

**Решение:** Проверь что токен бота правильный и бот не заблокирован @BotFather.

### Уведомления не приходят

**Решение:** Проверь настройки `NOTIFICATION_HOUR` и `NOTIFICATION_MINUTE` в `.env`.

## 📚 Дополнительная информация

### Требования к окружению

- Python 3.8 или выше
- MySQL 8.0 или выше
- 50MB свободного места на диске

### Best Practices

- Модульная архитектура для легкого расширения
- Separation of Concerns (разделение ответственности)
- DRY (Don't Repeat Yourself)
- Comprehensive error handling
- Detailed logging

## 👨‍💻 Автор

**shxpe0x**
- GitHub: [@shxpe0x](https://github.com/shxpe0x)

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🙏 Благодарности

- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI) за отличную библиотеку
- Преподавателям колледжа за знания по Python и MySQL

---

⭐ Если проект понравился, поставь звезду!
